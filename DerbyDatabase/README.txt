This is a Java database project, using the DerbyDatabase or JavaDB. Install JavaDB and start the DerbyDatabase Server. Compile the files in the src/tryit folder. Run the Java program using eclipse or command line. The TechTest program reads Questions in the Java subject, from the database, populates the questions in Java Objects, displays the questions and scores the test.